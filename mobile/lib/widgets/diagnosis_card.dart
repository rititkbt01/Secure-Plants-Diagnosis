/// Diagnosis Card Widget
///
/// Owner: Jeshik Neupane
///
/// Compact card for displaying a diagnosis result in the history list.
/// Shows: disease name, crop, confidence, date.
///
/// TODO (Jeshik): Implement card UI

import 'package:flutter/material.dart';

class DiagnosisCard extends StatelessWidget {
  // final DiagnosisResult result;

  const DiagnosisCard({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO (Jeshik): Implement compact diagnosis card
    return const Placeholder();
  }
}
